test:
<% if ENV['DB'] == 'mysql' %>
  adapter: mysql2
  encoding: utf8mb4
  collation: utf8mb4_unicode_ci
  username: travis
  password:
  database: persistent_hash_test
<% elsif ENV['DB'] == 'pgsql' %>
  adapter: postgresql
  database: persistent_hash_test
  username: travis
  password:
<%
  else
    raise "unknown DB value '#{ENV['DB']}'"
  end
%>
